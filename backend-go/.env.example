# All required environment variable values are provided in the attached document.
# This file serves as a reference for the expected environment keys.

# Database
DATABASE_URL=

# Email (Brevo SMTP Configuration)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_SENDER=
SMTP_NOTIFY_TO=
